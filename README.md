
# QA API Testing

Учебный проект, включающий разработку и тестирование REST API для работы с пользователями.

API было самостоятельно разработано на Node.js и Express с использованием MySQL.

После реализации API было проведено функциональное и негативное тестирование с использованием Postman и SQL.

В проекте проверяются позитивные и негативные сценарии, валидация входных данных, HTTP status codes, обработка ошибок, уникальность email и соответствие данных API данным в базе MySQL.

Проект выполнен для практического применения навыков разработки REST API, тестирования API, работы с базами данных и подготовки портфолио Junior QA Engineer.


## О проекте

В рамках проекта проводится функциональное и негативное тестирование REST API.

API позволяет:

* получать список пользователей;
* создавать пользователей;
* изменять данные пользователей;
* удалять пользователей.

Тестирование выполняется с использованием Postman, SQL и MySQL.

## 👩‍💻 Моя роль в проекте

В рамках проекта я самостоятельно:

* разработала REST API на Node.js и Express;
* написала серверную логику для работы с пользователями;
* реализовала endpoints `GET`, `POST`, `PATCH` и `DELETE`;
* реализовала валидацию входных данных;
* добавила проверку возраста пользователя;
* реализовала проверку уникальности email;
* подключила MySQL и работу с базой данных;
* проводила ручное тестирование API в Postman;
* создавала позитивные и негативные тестовые сценарии;
* добавляла автоматические проверки с использованием `pm.test()`;
* выполняла проверки данных через SQL;
* проверяла соответствие данных между API и MySQL;
* использовала Git и GitHub для хранения и ведения проекта.

Проект разработан и протестирован мной в рамках практики навыков Backend/API и подготовки к позиции Junior QA Engineer.


## Технологии и инструменты

* Node.js
* Express
* MySQL
* MySQL Workbench
* Postman
* SQL
* Git
* GitHub

## API endpoints

| Method | Endpoint     | Description                    |
| ------ | ------------ | ------------------------------ |
| GET    | `/users`     | Получение списка пользователей |
| POST   | `/users`     | Создание пользователя          |
| PATCH  | `/users/:id` | Изменение данных пользователя  |
| DELETE | `/users/:id` | Удаление пользователя          |

## Что тестируется

В проекте проверяются:

* позитивные сценарии;
* негативные сценарии;
* валидация входных данных;
* граничные значения;
* HTTP status codes;
* обработка ошибок;
* уникальность email;
* работа с базой данных;
* соответствие данных API данным в MySQL;
* автоматические проверки в Postman.

## Тестирование POST /users

На данный момент протестированы следующие сценарии:

* создание пользователя с валидными данными;
* создание пользователя с уже существующим email;
* проверка минимального допустимого возраста;
* проверка максимального допустимого возраста;
* проверка возраста ниже допустимого диапазона;
* проверка возраста выше допустимого диапазона;
* создание пользователя без имени;
* создание пользователя без email.

Проверяемые HTTP status codes:

* `201 Created` — успешное создание пользователя;
* `400 Bad Request` — некорректные или отсутствующие данные;
* `409 Conflict` — попытка использовать существующий email.

Для части негативных сценариев используются автоматические проверки Postman с помощью JavaScript.

## Postman

Коллекция Postman находится в файле:

`QA_API.postman_collection.json`

Коллекцию можно импортировать непосредственно в Postman.

Автоматические проверки выполняются с помощью `pm.test()`.

Пример проверок:

* HTTP status code;
* сообщение об ошибке;
* корректность данных ответа.

## Работа с базой данных

Для хранения данных используется MySQL.

Проверяется соответствие данных между API и базой данных.

Используемые SQL-инструменты:

* `SELECT`
* `WHERE`
* `ORDER BY`
* `INSERT`
* `UPDATE`
* `DELETE`

## Структура проекта

```text
qa-api-testing/
│
├── .gitignore
├── README.md
├── QA_API.postman_collection.json
├── index.js
├── package.json
└── package-lock.json
```

## Как запустить API

Установить зависимости:

```bash
npm install
```

Запустить сервер:

```bash
npm start
```

После запуска API доступно по адресу:

```text
http://localhost:3000
```

## Статус проекта

В разработке и тестировании.

Текущий этап:

* API реализовано;
* подключена MySQL;
* создана коллекция Postman;
* добавлены автоматические проверки;
* реализовано тестирование POST `/users`;
* дальнейшее тестирование GET, PATCH и DELETE выполняется по мере развития проекта.
